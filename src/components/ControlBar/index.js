import React from 'react';

export default function ControlBar() {
    return (
        <div className="inner-wrapper flex flex-col items-center text-center">
            <p>Using a keyboard? Press keys A-Z to play.</p>
        </div>
    );
}
